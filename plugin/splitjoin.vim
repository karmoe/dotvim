" We will define our own mappings, so disable the default ones (gJ and gS)
let g:splitjoin_split_mapping = ''
let g:splitjoin_join_mapping = ''

" Affects all the arrows after the cursor - single line for each
let splitjoin_php_method_chain_full = 1
